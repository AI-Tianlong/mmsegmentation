conda create -n atl-py10-zgj python=3.10.15

conda activate atl-py10-zgj
conda install gdal

conda remove pytorch torchvision torchaudio -y
pip install torch==2.3.1 torchvision==0.18.1 torchaudio==2.3.1 --index-url https://download.pytorch.org/whl/cu121

# 先安装一个包含 pkg_resources 的旧版本
pip install "setuptools<70.0.0"
# 然后再次尝试安装（同样建议带上 --no-build-isolation）
pip install -e . --no-build-isolation

cd mmcv
cd mmengine 
cd mmdetection
cd mmpretrain
cd mmsegmentation

pip install -e . --no-build-isolation
pip install timm==1.0.9
pip install transformers==4.44.1
pip install flash_attn==0.2.8 --no-build-isolation

pip install regex ftfy ATL_Tools


cd mmsegmentation 

bash tool

推理命令
bash too

训练命令
 CUDA_VISIBLE_DEVICES=4,5,6,7 bash tools/dist_train.sh /opt/workspace/AI-Tianlong/openmmlab/mmsegmentation/configs_new/ATL-paper-new-hiera-6-20251230-修正upernet通道/baseline-GF2/part2-baseline-GF2-convnext-L-upernet-baseline-640x640-第三篇文章.py 4

