# 消融0：三个通道分别输出，没有交互，只计算celoss3 其他的不要，不管
```python
def forward(self, inputs):
    """Forward function."""
    # [[4, 64, 128, 128], [4, 128, 64, 64],[4, 320, 32, 32],[4, 512, 16, 16]]
    # in_index = [1,2,3] 
    embedding = self.proj_head(inputs[-1])  # [4,512,16,16]-->[4,256,16,16]
    # import pdb; pdb.set_trace()
    inputs = self._transform_inputs(inputs)
    # [[4, 128, 64, 64],[4, 320, 32, 32],[4, 512, 16, 16]]
    inputs = [
        resize(
            level,
            size=inputs[0].shape[2:],
            mode='bilinear',
            align_corners=self.align_corners) for level in inputs
    ]
    # [[4, 128, 64, 64],[4, 320, 64, 64],[4, 512, 64, 64]]
    # import pdb; pdb.set_trace()
    inputs = torch.cat(inputs, dim=1)  # [4,960,64,64]
    # apply a conv block to squeeze feature map
    x = self.squeeze(inputs)  # [4,1024,64,64]
    # apply hamburger module
    x = self.hamburger(x)     # [4,1024,64,64]

    # apply a conv block to align feature map
    output = self.align(x) # [4, 1024, 64, 64] --> [4, 1024, 64, 64]
    

    output_L1 = self.cls_seg(output, self.conv_seg_L1)        # [4, 1024, 64, 64] --> [4, 5, 64, 64]

    output_L2 = self.cls_seg(output, self.conv_seg_L2)        # [4, 1024, 64, 64] --> [4, 10, 64, 64]
    
    output_L3 = self.cls_seg(output, self.conv_seg) # [4, 1024, 64, 64] --> [4, 19, 64, 64]
    
    output_list = [output_L1, output_L2, output_L3]
    self.step += 1

    return output_list, embedding
```

# 消融1：三个通道分别输出，没有交互，三个单独celoss
```python
def forward(self, inputs):
    """Forward function."""
    # [[4, 64, 128, 128], [4, 128, 64, 64],[4, 320, 32, 32],[4, 512, 16, 16]]
    # in_index = [1,2,3] 
    embedding = self.proj_head(inputs[-1])  # [4,512,16,16]-->[4,256,16,16]
    # import pdb; pdb.set_trace()
    inputs = self._transform_inputs(inputs)
    # [[4, 128, 64, 64],[4, 320, 32, 32],[4, 512, 16, 16]]
    inputs = [
        resize(
            level,
            size=inputs[0].shape[2:],
            mode='bilinear',
            align_corners=self.align_corners) for level in inputs
    ]
    # [[4, 128, 64, 64],[4, 320, 64, 64],[4, 512, 64, 64]]
    # import pdb; pdb.set_trace()
    inputs = torch.cat(inputs, dim=1)  # [4,960,64,64]
    # apply a conv block to squeeze feature map
    x = self.squeeze(inputs)  # [4,1024,64,64]
    # apply hamburger module
    x = self.hamburger(x)     # [4,1024,64,64]

    # apply a conv block to align feature map
    output = self.align(x) # [4, 1024, 64, 64] --> [4, 1024, 64, 64]
    

    output_L1 = self.cls_seg(output, self.conv_seg_L1)        # [4, 1024, 64, 64] --> [4, 5, 64, 64]

    output_L2 = self.cls_seg(output, self.conv_seg_L2)        # [4, 1024, 64, 64] --> [4, 10, 64, 64]
    
    output_L3 = self.cls_seg(output, self.conv_seg) # [4, 1024, 64, 64] --> [4, 19, 64, 64]
    
    output_list = [output_L1, output_L2, output_L3]
    self.step += 1

    return output_list, embedding
```


# 消融2：三个通道有个1x1的空间注意力卷积交互，太简单了
```python
# 消融实验1：1x1 conv 空间注意力
def forward(self, inputs):
    """Forward function."""
    # [[4, 64, 128, 128], [4, 128, 64, 64],[4, 320, 32, 32],[4, 512, 16, 16]]
    # in_index = [1,2,3] 
    embedding = self.proj_head(inputs[-1])  # [4,512,16,16]-->[4,256,16,16]
    # import pdb; pdb.set_trace()
    inputs = self._transform_inputs(inputs)
    # [[4, 128, 64, 64],[4, 320, 32, 32],[4, 512, 16, 16]]
    inputs = [
        resize(
            level,
            size=inputs[0].shape[2:],
            mode='bilinear',
            align_corners=self.align_corners) for level in inputs
    ]
    # [[4, 128, 64, 64],[4, 320, 64, 64],[4, 512, 64, 64]]
    # import pdb; pdb.set_trace()
    inputs = torch.cat(inputs, dim=1)  # [4,960,64,64]
    # apply a conv block to squeeze feature map
    x = self.squeeze(inputs)  # [4,1024,64,64]
    # apply hamburger module
    x = self.hamburger(x)     # [4,1024,64,64]

    # apply a conv block to align feature map
    output = self.align(x) # [4, 1024, 64, 64] --> [4, 1024, 64, 64]
    

    output_L1 = self.cls_seg(output, self.conv_seg_L1)        # [4, 1024, 64, 64] --> [4, 5, 64, 64]
    output_L1_attentation = self.conv_attention_L1(output_L1) # [4, 5, 64, 64] --> [4, 1, 64, 64]
    output = output + output_L1_attentation # [4, 1024, 64, 64] + [4, 1, 64, 64]--> [4, 1024, 64, 64] add L1特征图

    output_L2 = self.cls_seg(output, self.conv_seg_L2)        # [4, 1024, 64, 64] --> [4, 10, 64, 64]
    output_L2_attentation = self.conv_attention_L2(output_L2) # [4, 10, 64, 64] --> [4, 1, 64, 64]
    output = output + output_L2_attentation # [4, 1024, 64, 64]+[4, 1, 64, 64] -->  [4, 1024, 64, 64] add L2特征图

    output_L3 = self.cls_seg(output, self.conv_seg) # [4, 1024, 64, 64] --> [4, 19, 64, 64]
    
    output_list = [output_L1, output_L2, output_L3]
    self.step += 1

    return output_list, embedding
```

# 消融3：生成了[2,5,64,64]的特征图，直接把这个特征图加到[2,1024,64,64]，再去生成L2的特征图，然后再cat到[2,1024,64,64]+[2,5,64,64],再生成L3的特征图，三个celoss
```python
    def forward(self, inputs):
        """Forward function."""
        # [[4, 64, 128, 128], [4, 128, 64, 64],[4, 320, 32, 32],[4, 512, 16, 16]]
        # in_index = [1,2,3] 
        embedding = self.proj_head(inputs[-1])  # [4,512,16,16]-->[4,256,16,16]
        # import pdb; pdb.set_trace()
        inputs = self._transform_inputs(inputs)
        # [[4, 128, 64, 64],[4, 320, 32, 32],[4, 512, 16, 16]]
        inputs = [
            resize(
                level,
                size=inputs[0].shape[2:],
                mode='bilinear',
                align_corners=self.align_corners) for level in inputs
        ]
        # [[4, 128, 64, 64],[4, 320, 64, 64],[4, 512, 64, 64]]
        # import pdb; pdb.set_trace()
        inputs = torch.cat(inputs, dim=1)  # [4,960,64,64]
        # apply a conv block to squeeze feature map
        x = self.squeeze(inputs)  # [4,1024,64,64]
        # apply hamburger module
        x = self.hamburger(x)     # [4,1024,64,64]

        # apply a conv block to align feature map
        output = self.align(x) # [4, 1024, 64, 64] --> [4, 1024, 64, 64]
        
        # import pdb; pdb.set_trace()
        output_L1 = self.cls_seg(output, self.conv_seg_L1)         # [4, 512, 64, 64] --> [4, 5, 64, 64]
        output = torch.cat([output, output_L1], dim=1)             # [4, 512, 64, 64] + [4, 5, 64, 64]--> [4, 517, 64, 64] add L1特征图

        output_L2 = self.cls_seg(output, self.conv_seg_L2)         # [4, 517, 64, 64] --> [4, 10, 64, 64]
        output = torch.cat([output, output_L2], dim=1)             # [4, 517, 64, 64] + [4, 10, 64, 64] --> [4, 527, 64, 64]
                   
        output_L3 = self.cls_seg(output, self.conv_seg_L3)            # [4, 527, 64, 64] --> [4, 19, 64, 64]
        
        output_list = [output_L1, output_L2, output_L3]
        self.step += 1

        return output_list, embedding
```

## 消融4：1x1 conv的卷积+treetriple loss
